#include <atcoder/all>
#include <bits/stdc++.h>
using namespace std;
using namespace atcoder;
using mint = modint1000000007;
using ll = long long;
int main() {
  int N, M;
  cin >> N >> M;
  vector<int> f(N);
  for (int i = 0; i < N; i++) cin >> f[i];
  auto dfs = [&](int l) -> ll {
    while ()
  };
}